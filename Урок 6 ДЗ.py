'''
ДЗ
Условие:
Есть кортеж data = ("Анна", 28, "Москва"). Распакуй его в переменные name, age, city.
Оберни распаковку в try/except и перехвати ValueError. 
Если ошибка — выведи: Ошибка распаковки: неверный формат данных.

Проверь также вариант, когда data = ("Борис", 30) (не хватает одного элемента).
'''
# #data = ("Анна", 28, "Москва")
# #data = ('Борис', 30)
# try:
#     name, age, city = data
# except:
#     print('Ошибка распаковки: неверный формат данных')
# #print(name)

'''
ДЗ
Условие:
Напиши функцию collect_numbers(*args), которая принимает любое количество аргументов 
и возвращает список только тех, которые являются числами (int или float).
Вызови её с разными типами: collect_numbers(10, "abc", 3.5, None, 7).
'''
#########################
'''
ДЗ
Условие:
Дан список values = [100, 20, 5, 0, 8].
Распакуй так: first, second, *rest = values.
Посчитай среднее для rest. Если в rest есть ноль и ты пытаешься использовать его как делитель 
(например, в каком‑то расчёте), корректно обработай ошибку и выведи понятное сообщение.
Для простоты: посчитай сумму rest и подели на длину rest. 
Но дополнительно сделай проверку: если длина rest равна 0 — выведи Делить не на что.
'''
# #values = [100, 20, 5, 0, 8]
# # values = [23, 567]
# first, second, *rest = values
# try:
#     print(round(sum(rest)/len(rest),1))
# except:
#     print('Делить не на что')

'''
ДЗ
Цель: научиться передавать словарь как именованные аргументы 
через **kwargs и проверять наличие обязательных ключей.

Условие:
Напиши функцию create_user(**kwargs), которая ожидает ключи name и age.
Если их нет — выброси KeyError с понятным сообщением.
Функция возвращает строку: Пользователь: <name>, возраст: <age>.
Вызови функцию двумя способами:

create_user(name="Мария", age=25)

create_user(name="Иван") (должно вызвать ошибку).
'''
def create_user(**kwargs):
    print(f'Пользователь:{}, возраст:{}')

create_user(name="Мария", age=25)
#create_user(name="Иван") 
'''
ДЗ
Цель: распаковать кортежи в цикле и обработать ValueError при преобразовании строки в число.

Условие:
Есть список кортежей records = [("Анна", "25"), ("Борис", "тридцать"), ("Виктор", "30")].
В цикле распакуй каждый кортеж в name, age_str.
Попробуй преобразовать age_str в int. Если не получится — выведи сообщение: 
Ошибка: возраст для <name> не является числом, но не прерывай цикл.
Для корректных записей выведи: <name>: <age>.
'''

'''
ДЗ
Условие:
Напиши функцию make_order(customer, *items, tax_rate=0.2, **options):

customer — имя клиента (обязательно).

*items — список товаров (строки).

tax_rate — ставка налога (по умолчанию 0.2).

В **options могут быть: discount (число от 0 до 1), currency (строка).

Логика:

Если items пустой — выбросить ValueError("Нет товаров в заказе").

Если tax_rate < 0 — выбросить ValueError("Ставка налога не может быть отрицательной").

Если есть discount и он не в диапазоне [0,1] — выбросить ValueError("Скидка должна быть от 0 до 1").

Вернуть словарь с итогами: {"customer": ..., "items": [...], "tax_rate": ..., "discount": ..., "currency": ...}.

Протестируй 3 вызова:

Нормальный заказ.

Заказ без товаров.

Заказ с отрицательным tax_rate.
'''